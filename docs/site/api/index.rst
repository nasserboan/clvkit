API Reference
=============

Every public class and function, documented from the source. Pick the half you
need: the timing models, the spend model, their composition into lifetime value,
the descriptive cohort tools, the shared contract they all read, or the plotting
functions behind every ``.plot()``.

.. toctree::
   :maxdepth: 2

   transaction_models
   spend
   lifetime_value
   cohorts
   core
   plotting
