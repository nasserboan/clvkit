Plotting
========

Every result's ``.plot()`` delegates to one of these. Call them directly when you
want an option the method shortcut doesn't expose, or an axis to draw onto.

.. currentmodule:: clvkit.plotting

.. autosummary::
   :toctree: generated/
   :nosignatures:

   plot_clv
   plot_prediction
   plot_probability_alive
   plot_parameter_uncertainty
   plot_cohort_matrix
   plot_survival_curve
   plot_independence
